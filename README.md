# Better-Instance-LUA
A better, and more efficient way to create instances.

### Example

```
local CoinsValue = Instance.new("IntValue", {
  Name = "Coins_Value",
  Value = 120,
  Parent = leaderstats,
  -- Index = Data
})
  ```
